

<section class="one-complement-container">
  {{#each element}}
  <div>
    <h1>{{top_complement_name}}</h1>
    <img src="{{image}}" alt="Top Complement Image" />
    <form action="{{_id}}" method="post">
      <button>Delete Complement</button>
    </form>
    <form action="/top-complements" method="get">
      <button>Go Back</button>
    </form>
  </div>
  {{/each}}  
</section>
