

<section class="one-complement-container">
  {{#each element}}
  <div>
    <h1>{{hoodie_complement_name}}</h1>
    <img src="{{image}}" alt="Hoodie Complement Image" />
    <form action="{{_id}}" method="post">
      <button>Delete Complement</button>
    </form>
    <form action="/hoodie-complements" method="get">
      <button>Go Back</button>
    </form>
  </div>
  {{/each}}  
</section>
